# InterviewAce Frontend

React interface for the AI mock interview platform.

## Commands

```bash
npm install
npm run dev
npm run build
```

## Required Environment

Copy `.env.example` to `.env` and update:

```env
VITE_API_URL=http://localhost:5000
```
